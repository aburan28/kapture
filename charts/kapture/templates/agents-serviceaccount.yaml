{{- if and .Values.agents.enabled .Values.agents.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kapture.agents.serviceAccountName" . }}
  namespace: {{ include "kapture.namespace" . }}
  labels:
    {{- include "kapture.agents.labels" . | nindent 4 }}
  {{- with .Values.agents.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
