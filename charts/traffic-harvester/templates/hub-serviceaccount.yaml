{{- if and .Values.hub.enabled .Values.hub.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "traffic-harvester.hub.serviceAccountName" . }}
  namespace: {{ include "traffic-harvester.namespace" . }}
  labels:
    {{- include "traffic-harvester.hub.labels" . | nindent 4 }}
  {{- with .Values.hub.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}

